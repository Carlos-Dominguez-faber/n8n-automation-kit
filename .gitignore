# Credenciales — NUNCA subir
.env
.env.local
.env.*
*api-key*
*api_key*
*.credentials

# MCP con credenciales reales — el .mcp.json del repo es el template
# Descomenta si quieres tener un .mcp.json local con tus credenciales
# y no quieres que se suba:
# .mcp.json

# Workflows personales — no se comparten (tienen IDs de credenciales específicos)
workflows/*.json
!workflows/starters/*.json

# macOS
.DS_Store

# Node
node_modules/
npm-debug.log*

# Claude Code — cache local (no relevante para el repo)
.claude/cache/
.claude/history.jsonl
.claude/sessions/

# Playwright artifacts
test-results/
playwright-report/
screenshots/*.png

# Logs y temporales
*.log
*.tmp
